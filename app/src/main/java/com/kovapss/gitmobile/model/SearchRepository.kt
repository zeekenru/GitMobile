package com.kovapss.gitmobile.model

import retrofit2.http.Query
import javax.inject.Inject


class SearchRepository {
//    @Inject lateinit var apiService: SearchService
//
//    fun searchRepositories(@Query("q") query: String,
//                           @Query("sort") sort: String,
//                           @Query("order") order: String) = apiService.searchRepositories(query, sort, order)
//
//
//    fun searchCommits(@Query("q") query: String,
//                      @Query("sort") sort: String,
//                      @Query("order") order: String) = apiService.searchCommits(query, sort, order)
//
//
//    fun searchCode(@Query("q") query: String,
//                   @Query("order") order: String) = apiService.searchCode(query, order)
//
//
//    fun searchIssues(@Query("q") query: String,
//                     @Query("sort") sort: String,
//                     @Query("order") order: String) = apiService.searchIssues(query, sort, order)
//
//
//    fun searchUsers(@Query("q") query: String,
//                    @Query("sort") sort: String,
//                    @Query("order") order: String) = apiService.searchUsers(query, sort, order)
}